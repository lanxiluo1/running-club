# 校园跑团训练管理系统 - 启动说明

## 项目简介

本系统是一款面向高校跑步社团的轻量化训练管理Web平台，解决传统跑团依赖微信群打卡、数据杂乱、无法量化训练成果、管理成本高、无系统化训练监督的痛点。

## 功能模块

- **用户中心**：登录注册、个人档案、角色权限
- **训练打卡**：图片上传+AI智能识别（OCR）、手动补填、审核机制
- **数据看板**：训练趋势图、速度分析、心率-速度关联图
- **社团生态**：排行榜、活动报名、训练计划
- **管理后台**：成员管理、数据审核、公告发布、数据导出

## 技术栈

- **前端**：Vue3 + Element Plus + ECharts + Vite
- **后端**：FastAPI + SQLAlchemy + SQLite
- **OCR识别**：EasyOCR（需要安装）

---

## 快速启动

### 1. 启动后端

```bash
cd backend

# 安装依赖（如果还未安装）
pip install -r requirements.txt

# 启动服务
python run.py
```

后端启动后运行在 http://localhost:8000

### 2. 启动前端

```bash
cd frontend

# 安装依赖（如果还未安装）
npm install

# 启动开发服务器
npm run dev
```

前端启动后运行在 http://localhost:5173

---

## 初始设置

### 创建管理员账号

1. 在前端页面注册一个普通账号
2. 通过 SQLite 数据库管理工具（如 SQLiteStudio、DB Browser）打开 `backend/running_club.db`
3. 执行以下 SQL 将该账号设为管理员：

```sql
UPDATE users SET role = 'admin' WHERE student_id = '你的学号';
```

### 或者通过命令行

```bash
cd backend
sqlite3 running_club.db
```

然后执行 UPDATE 语句。

---

## 访问地址

| 服务 | 地址 |
|------|------|
| 前端页面 | http://localhost:5173 |
| 后端API | http://localhost:8000 |
| API文档 | http://localhost:8000/docs |

---

## 项目结构

```
d:/AI项目库/飞行国度训练/
├── backend/                    # 后端项目
│   ├── app/
│   │   ├── models/           # 数据库模型
│   │   ├── schemas/           # Pydantic 数据模型
│   │   ├── routers/          # API 路由
│   │   ├── services/          # 业务逻辑
│   │   └── utils/            # 工具函数
│   ├── requirements.txt       # Python 依赖
│   ├── run.py                # 启动脚本
│   └── running_club.db        # SQLite 数据库
│
├── frontend/                   # 前端项目
│   ├── src/
│   │   ├── api/              # API 调用
│   │   ├── router/            # 路由配置
│   │   ├── stores/            # 状态管理
│   │   ├── views/             # 页面组件
│   │   └── main.js            # 入口文件
│   ├── package.json
│   └── vite.config.js
│
├── 校园跑团训练管理系统 PRD产品需求文档.md
└── 启动说明.md
```

---

## 常见问题

### 1. pip install 失败

确保使用 Python 3.7+：
```bash
python --version
pip install -r requirements.txt
```

### 2. npm install / npm run dev 失败

确保安装了 Node.js：
```bash
node --version
npm --version
```

如果提示"无法识别 npm/node"，需要将 Node.js 添加到系统 PATH：
1. 按 `Win + R`，输入 `sysdm.cpl`，回车
2. 点击 **高级** → **环境变量**
3. 在 **系统变量** 中找到 **Path**，双击编辑
4. 点击 **新建**，添加 `C:\Program Files\nodejs`
5. 确定保存后，**重新打开终端**

### 3. 前端无法访问后端API

确保后端服务运行在 8000 端口，Vite 代理已配置。

### 4. OCR 识别不工作

OCR 功能需要安装 EasyOCR 相关依赖。如果不需要 OCR 功能，系统仍可正常使用手动输入模式。

---

## 数据说明

- **速度单位**：km/h（千米/小时）
- **距离单位**：km（千米）
- **时长单位**：秒
- **打卡状态**：待审核(pending) → 已通过(approved) / 已驳回(rejected)
- **用户角色**：member（普通成员）/ admin（管理员）
